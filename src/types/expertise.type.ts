export type TCommonCard = {
    id: string;
    title: string;
    description: string;
}

export type Technology = {
    id: string;
    image: string;
    title: string;
    description: string;
}

export type Articles = {
    id: number;
    title: string;
    description: string;
    image: string;
}